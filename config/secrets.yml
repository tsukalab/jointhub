# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.
shared:
  facebook_client_id: <%= ENV['FACEBOOK_CLIENT_ID'] %>
  facebook_client_secret: <%= ENV['FACEBOOK_CLIENT_SECRET'] %>

development:
  secret_key_base: e3460bada07aef2db08598b0b18a69d68ee04ce99f6d0e659b0d6fcc2b8e16f2adc637e9836f89ec5e9335aab73cee5842b6ed3907fa9e6e9efb2957980ab086

test:
  secret_key_base: fa892b1914b37dcd16645c599342f9a02fe95556be415d395a8d8e6190fef283f2186bd661db3bcaa81f3522bb29de1460d959d2b8e30f1584d87f5ae795ba36

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
