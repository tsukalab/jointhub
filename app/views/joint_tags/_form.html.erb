<%= form_for(joint_tag) do |f| %>
  <% if joint_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(joint_tag.errors.count, "error") %> prohibited this joint_tag from being saved:</h2>

      <ul>
      <% joint_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :joint_id %>
    <%= f.text_field :joint_id %>
  </div>

  <div class="field">
    <%= f.label :tag_id %>
    <%= f.text_field :tag_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
