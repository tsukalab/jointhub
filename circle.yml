machine:
  timezone:
    Japan
  ruby:
    version: 2.3.3

test:
  override:
    - echo 'skip test'

deployment:
  production:
    branch: master
    commands:
      - bundle exec cap production deploy
