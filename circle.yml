machine:
  timezone:
    Japan
  ruby:
    version: 2.4.0

test:
  override:
    - echo 'skip test'

deployment:
  production:
    branch: master
    commands:
      - bundle exec cap production deploy
